type User {
    id: ID!
    password: String!
    "information"
    fullName: String!
    phone: Int
    email: String!
    image: String
    enable: Boolean!
    verificationCode: String
    role: Role
    cardId: [ID]
    prodBoughtId: [ID]
}

type CreateCusResponse {
    code: Int!
    success: Boolean!
    message: String!
    user: User
}



enum Role {
    ADMIN
    USER
}
